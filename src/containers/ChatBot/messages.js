import { defineMessages } from 'react-intl';

export const scope = 'src.containers.ChatBot';

export default defineMessages({
  chatBot: {
    id: `${scope}.chatBot`,
    defaultMessage: 'ChatBot',
  }
});
